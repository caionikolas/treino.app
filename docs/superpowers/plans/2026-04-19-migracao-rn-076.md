# Migração para React Native 0.76 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrar o projeto Treino.app de React Native 0.85.1 para React Native 0.76.x (última versão com suporte sólido à arquitetura antiga), preservando todo o código em `src/`, assets, docs e history git. Desbloqueia vídeo real, top tabs material, e track-player (Fase 6).

**Architecture:** Scaffold fresh RN 0.76 em /tmp, substituir a shell Android e os configs root no treino.app, preservar `src/`, `assets/`, `docs/`, `__tests__/`, `__mocks__/`, `.git/`. Reinstalar deps nas versões compatíveis. Desativar new arch. Restaurar customizações (paths, mp4 assets, native KeepAwake module, permissões, vector-icons gradle).

**Tech Stack:** React Native 0.76.x, React 18.3.1, TypeScript, op-sqlite, Zustand, React Navigation, better-sqlite3 (devDep), @notifee/react-native, react-native-video, react-native-pager-view, react-native-calendars.

**Spec de referência:** `docs/superpowers/specs/2026-04-19-migracao-rn-076-design.md`

**Convenções:**
- Executar cada task em ordem (ordem importa pra esta migração)
- Commit após cada task que estabiliza
- `pnpm tsc --noEmit` clean é check de sanidade; se quebrar, tem que consertar antes de prosseguir
- Teste em device só nos Tasks 10+ (build Android)

---

## Task 1: Verificar estado atual + criar branch de backup

**Files:** nenhum (git)

- [ ] **Step 1: Verificar working tree limpa**

Run: `git status`
Expected: `nothing to commit, working tree clean`. Se houver mudanças, commitar ou stashar antes.

- [ ] **Step 2: Criar branch de backup de segurança**

```bash
git branch backup-pre-rn-076-migration
```

A migração é destrutiva em `android/`, `package.json`, etc. Se algo crítico falhar, `git reset --hard backup-pre-rn-076-migration` reverte tudo.

- [ ] **Step 3: Salvar versão atual do Node modules local (opcional)**

Não precisa mover nada; só anotar: `node_modules/` será recriado. `pnpm-lock.yaml` será regenerado.

- [ ] **Step 4: Rodar tests pra baseline**

Run: `pnpm test`
Expected: 42 tests pass. Anotar número pra comparar depois da migração.

---

## Task 2: Scaffold RN 0.76 em /tmp

**Files:** `/tmp/TreinoApp76/` (temporário)

- [ ] **Step 1: Limpar /tmp se já tiver o diretório**

```bash
rm -rf /tmp/TreinoApp76
```

- [ ] **Step 2: Criar projeto RN 0.76**

```bash
cd /tmp
npx @react-native-community/cli@latest init TreinoApp76 --version 0.76.6 --pm npm --skip-install
```

Expected: `/tmp/TreinoApp76/` criado com estrutura RN 0.76.

- [ ] **Step 3: Remover ios/ (não vamos usar)**

```bash
rm -rf /tmp/TreinoApp76/ios
```

- [ ] **Step 4: Inspecionar a shell gerada**

```bash
ls /tmp/TreinoApp76/
cat /tmp/TreinoApp76/package.json | head -30
```

Expected: arquivos `android/`, `App.tsx`, `index.js`, `package.json`, `babel.config.js`, `metro.config.js`, `tsconfig.json`, `jest.config.js`, `.watchmanconfig`, `.prettierrc.js`, `.eslintrc.js`, `.bundle/`, `.gitignore`, `Gemfile`, `README.md`. `package.json` com `"react-native": "0.76.x"`.

---

## Task 3: Substituir shell Android e configs root

**Files:**
- Modify: `android/` (full replace)
- Modify: `package.json`, `index.js`, `App.tsx`, `babel.config.js`, `metro.config.js`, `tsconfig.json`, `jest.config.js`
- Modify: `.watchmanconfig`, `.prettierrc.js`, `.eslintrc.js`, `.bundle/`

- [ ] **Step 1: Remover shell antiga (será substituída)**

```bash
cd /home/nikolas-dev/Dev/treino.app
rm -rf android .bundle
rm -f package.json index.js App.tsx babel.config.js metro.config.js tsconfig.json jest.config.js
rm -f .watchmanconfig .prettierrc.js .eslintrc.js Gemfile
rm -f pnpm-lock.yaml
rm -rf node_modules
```

**Não** mexer em: `src/`, `assets/`, `docs/`, `__tests__/`, `__mocks__/`, `.git/`, `.claude/`, `.npmrc`, `CLAUDE.md`, `README.md`, `ROADMAP.md`, `.gitignore`, `.gitattributes`.

- [ ] **Step 2: Copiar shell nova de /tmp**

```bash
cp -r /tmp/TreinoApp76/android ./android
cp /tmp/TreinoApp76/package.json ./package.json
cp /tmp/TreinoApp76/index.js ./index.js
cp /tmp/TreinoApp76/App.tsx ./App.tsx
cp /tmp/TreinoApp76/babel.config.js ./babel.config.js
cp /tmp/TreinoApp76/metro.config.js ./metro.config.js
cp /tmp/TreinoApp76/tsconfig.json ./tsconfig.json
cp /tmp/TreinoApp76/jest.config.js ./jest.config.js
cp /tmp/TreinoApp76/.watchmanconfig ./.watchmanconfig
cp /tmp/TreinoApp76/.prettierrc.js ./.prettierrc.js
cp /tmp/TreinoApp76/.eslintrc.js ./.eslintrc.js
cp /tmp/TreinoApp76/Gemfile ./Gemfile 2>/dev/null || true
cp -r /tmp/TreinoApp76/.bundle ./.bundle
```

- [ ] **Step 3: Limpar /tmp**

```bash
rm -rf /tmp/TreinoApp76
```

- [ ] **Step 4: Atualizar nome do projeto em `package.json`**

Abra `package.json`. O nome provavelmente é `"TreinoApp76"` — trocar para `"TreinoApp"` e adicionar `"version": "0.0.1"`, `"private": true`:

```json
{
  "name": "TreinoApp",
  "version": "0.0.1",
  "private": true,
  ...
}
```

- [ ] **Step 5: Atualizar package name no Android**

O scaffold criou o projeto como `com.treinoapp76`. Precisamos voltar pro `com.treinoapp` (para compatibilidade com APK instalado + our `KeepAwakeModule` Kotlin package).

Renomear diretório:
```bash
mv android/app/src/main/java/com/treinoapp76 android/app/src/main/java/com/treinoapp
# Remove subdir antigo
rmdir android/app/src/main/java/com 2>/dev/null || true
mkdir -p android/app/src/main/java/com
mv android/app/src/main/java/com/treinoapp android/app/src/main/java/com/treinoapp
```

(Esse mv já move pro lugar certo; os comandos acima cobrem ambos os casos.)

Substituir `com.treinoapp76` → `com.treinoapp` em:
```bash
cd android
grep -rl 'com.treinoapp76' . | xargs sed -i 's/com\.treinoapp76/com.treinoapp/g'
grep -rl 'TreinoApp76' . | xargs sed -i 's/TreinoApp76/TreinoApp/g'
cd ..
```

Verifique: `grep -r 'treinoapp76' android/` deve retornar vazio.

---

## Task 4: Restaurar customizações nos configs

**Files:**
- Modify: `metro.config.js`
- Modify: `babel.config.js`
- Modify: `tsconfig.json`
- Modify: `jest.config.js`
- Modify: `package.json`

- [ ] **Step 1: `metro.config.js` — registrar mp4 como asset**

Substituir o conteúdo por:

```javascript
const {getDefaultConfig, mergeConfig} = require('@react-native/metro-config');

const defaultConfig = getDefaultConfig(__dirname);
const { assetExts, sourceExts } = defaultConfig.resolver;

const config = {
  resolver: {
    assetExts: [...assetExts, 'mp4'],
    sourceExts,
  },
};

module.exports = mergeConfig(defaultConfig, config);
```

- [ ] **Step 2: `babel.config.js` — path alias**

Substituir por:

```javascript
module.exports = {
  presets: ['module:@react-native/babel-preset'],
  plugins: [
    [
      'module-resolver',
      {
        root: ['./'],
        alias: {
          '@': './src',
        },
      },
    ],
  ],
};
```

- [ ] **Step 3: `tsconfig.json` — paths + jest types**

Substituir conteúdo por:

```json
{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "types": ["jest"],
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
```

- [ ] **Step 4: `jest.config.js` — pnpm/mp4 adaptations**

Substituir conteúdo por:

```javascript
module.exports = {
  preset: 'react-native',
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
    '\\.mp4$': '<rootDir>/__mocks__/mp4Mock.js',
  },
  transformIgnorePatterns: [
    'node_modules/(?!(.pnpm/)?(@?)(jest-)?react-native(.*)?|@react-navigation|react-native-uuid)',
  ],
};
```

- [ ] **Step 5: `package.json` — adicionar `pnpm.onlyBuiltDependencies`**

Abra `package.json` e adicione (no fim do objeto, antes do `}` final):

```json
"pnpm": {
  "onlyBuiltDependencies": [
    "react-native-video",
    "react-native-pager-view",
    "@op-engineering/op-sqlite",
    "react-native-screens",
    "react-native-reanimated",
    "@notifee/react-native"
  ]
}
```

- [ ] **Step 6: `App.tsx` — substituir pelo nosso bootstrap**

O scaffold vem com código template. Substituir COMPLETAMENTE por (este é o mesmo do RN 0.85, funciona em 0.76):

```typescript
import React, { useEffect, useState } from 'react';
import { View, ActivityIndicator, StatusBar, StyleSheet, Text } from 'react-native';
import { SafeAreaProvider } from 'react-native-safe-area-context';
import { AppNavigator } from '@/navigation/AppNavigator';
import { runMigrations } from '@/database/migrations';
import { runSeeds } from '@/database/seeds/runSeeds';
import { useExerciseStore } from '@/store/useExerciseStore';
import { setupNotificationChannel } from '@/services/notificationService';
import { colors } from '@/theme';

function App(): React.JSX.Element {
  const [ready, setReady] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const loadExercises = useExerciseStore(s => s.load);

  useEffect(() => {
    (async () => {
      try {
        await runMigrations();
        await runSeeds();
        await loadExercises();
        await setupNotificationChannel();
        setReady(true);
      } catch (e) {
        console.error('Bootstrap failed', e);
        setError(e instanceof Error ? e.message : 'Erro desconhecido');
      }
    })();
  }, [loadExercises]);

  if (error) {
    return (
      <View style={styles.center}>
        <Text style={styles.errorText}>Erro ao iniciar o app:</Text>
        <Text style={styles.errorText}>{error}</Text>
      </View>
    );
  }

  if (!ready) {
    return (
      <View style={styles.center}>
        <ActivityIndicator size="large" color={colors.accent} />
      </View>
    );
  }

  return (
    <SafeAreaProvider>
      <StatusBar barStyle="light-content" backgroundColor={colors.primary} />
      <AppNavigator />
    </SafeAreaProvider>
  );
}

const styles = StyleSheet.create({
  center: { flex: 1, backgroundColor: colors.background, alignItems: 'center', justifyContent: 'center', padding: 24 },
  errorText: { color: colors.textPrimary, marginBottom: 8, textAlign: 'center' },
});

export default App;
```

---

## Task 5: Configurar Android (permissões, vector-icons, native module)

**Files:**
- Modify: `android/gradle.properties`
- Modify: `android/app/src/main/AndroidManifest.xml`
- Modify: `android/app/build.gradle`
- Create: `android/app/src/main/java/com/treinoapp/KeepAwakeModule.kt`
- Create: `android/app/src/main/java/com/treinoapp/KeepAwakePackage.kt`
- Modify: `android/app/src/main/java/com/treinoapp/MainApplication.kt` (ou .java)
- Modify: `android/app/src/main/java/com/treinoapp/MainActivity.kt` (ou .java)

- [ ] **Step 1: `android/gradle.properties` — desativar new arch**

Adicione (ou confirme que já existe):
```properties
newArchEnabled=false
hermesEnabled=true
```

Se outra linha tiver `newArchEnabled=true`, troque para `false`.

- [ ] **Step 2: `AndroidManifest.xml` — adicionar permissões**

Dentro de `<manifest>` antes de `<application>`, garantir que tem:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
```

- [ ] **Step 3: `android/app/build.gradle` — aplicar fonts.gradle do vector-icons**

No fim do arquivo (última linha), adicione:
```gradle
apply from: file("../../node_modules/react-native-vector-icons/fonts.gradle")
```

- [ ] **Step 4: Ajustar minSdkVersion (manter 26)**

Em `android/build.gradle`, bloco `buildscript.ext`, confirme `minSdkVersion = 26`. Se o scaffold veio com outro valor, troque para 26. Outras variáveis (buildToolsVersion, compileSdkVersion, targetSdkVersion, ndkVersion, kotlinVersion) mantenha o que o scaffold gerou.

- [ ] **Step 5: Criar `android/app/src/main/java/com/treinoapp/KeepAwakeModule.kt`**

```kotlin
package com.treinoapp

import android.view.WindowManager
import com.facebook.react.bridge.ReactApplicationContext
import com.facebook.react.bridge.ReactContextBaseJavaModule
import com.facebook.react.bridge.ReactMethod

class KeepAwakeModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaModule(reactContext) {
  override fun getName(): String = "KeepAwake"

  @ReactMethod
  fun activate() {
    val activity = reactApplicationContext.currentActivity ?: return
    activity.runOnUiThread {
      activity.window.addFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
    }
  }

  @ReactMethod
  fun deactivate() {
    val activity = reactApplicationContext.currentActivity ?: return
    activity.runOnUiThread {
      activity.window.clearFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
    }
  }
}
```

- [ ] **Step 6: Criar `android/app/src/main/java/com/treinoapp/KeepAwakePackage.kt`**

```kotlin
package com.treinoapp

import com.facebook.react.ReactPackage
import com.facebook.react.bridge.NativeModule
import com.facebook.react.bridge.ReactApplicationContext
import com.facebook.react.uimanager.ViewManager

class KeepAwakePackage : ReactPackage {
  override fun createNativeModules(reactContext: ReactApplicationContext): List<NativeModule> =
    listOf(KeepAwakeModule(reactContext))

  override fun createViewManagers(reactContext: ReactApplicationContext): List<ViewManager<*, *>> =
    emptyList()
}
```

- [ ] **Step 7: Registrar `KeepAwakePackage` em `MainApplication`**

Em RN 0.76 o arquivo provavelmente é `MainApplication.kt`. READ primeiro. Procure o método que retorna `List<ReactPackage>`, tipicamente `getPackages()`:

```kotlin
override fun getPackages(): List<ReactPackage> =
    PackageList(this).packages.apply {
      // add custom packages here
    }
```

Edite para incluir:
```kotlin
override fun getPackages(): List<ReactPackage> =
    PackageList(this).packages.apply {
      add(KeepAwakePackage())
    }
```

Se o formato for diferente (ex: propriedade em vez de método), adapte. Mantenha toda outra lógica intacta.

- [ ] **Step 8: Confirmar `MainActivity` e `MainApplication` usam package `com.treinoapp`**

Abra os dois arquivos e confirme a linha `package com.treinoapp`. Se ainda estiver `com.treinoapp76`, corrija (já deveria ter sido corrigido via sed no Task 3 Step 5).

---

## Task 6: Instalar dependências nas versões compatíveis

**Files:** `package.json`, `pnpm-lock.yaml` (recriado)

- [ ] **Step 1: Instalar core deps (navigation)**

```bash
pnpm add @react-navigation/native@^7.0.0 \
         @react-navigation/bottom-tabs@^7.0.0 \
         @react-navigation/native-stack@^7.0.0 \
         @react-navigation/material-top-tabs@^7.0.0 \
         react-native-screens@~3.35.0 \
         react-native-safe-area-context@~4.14.0 \
         react-native-pager-view@^7.0.0
```

- [ ] **Step 2: Instalar media, state, misc**

```bash
pnpm add @op-engineering/op-sqlite@^11.4.3 \
         react-native-video@^6.7.0 \
         zustand@^5.0.0 \
         react-native-vector-icons@^10.0.0 \
         react-native-uuid@^2.0.0 \
         @notifee/react-native@^9.0.0 \
         react-native-calendars@^1.1300.0
```

**Nota sobre op-sqlite:** versão ~11.4.3 é conhecida-boa para RN 0.74-0.77. Se `pnpm install` falhar ou build reclamar, tentar `~10.0.0` ou `~9.0.0` como fallback. Verificar se API continua async (o nosso repositório espera `await db.execute(...)`).

**Nota sobre @notifee:** versão 9.x continua suportando RN 0.76. Se der conflito, fallback `^7.8.2`.

- [ ] **Step 3: Instalar dev deps**

```bash
pnpm add -D babel-plugin-module-resolver@^5.0.0 \
            better-sqlite3 \
            @types/better-sqlite3
```

- [ ] **Step 4: Executar approve-builds manualmente (pnpm bloqueia scripts por default)**

```bash
cd node_modules/better-sqlite3 && npm run install 2>&1 | tail -3 ; cd -
```

Expected: better-sqlite3 compila. Se falhar, documenta o erro e prossegue (pode não ser crítico se já estava compilado antes — checar `node_modules/better-sqlite3/build/`).

- [ ] **Step 5: Verificar versão final do RN**

```bash
python3 -c "import json;p=json.load(open('node_modules/react-native/package.json'));print('rn:',p.get('version'));p=json.load(open('node_modules/react/package.json'));print('react:',p.get('version'))"
```
Expected: RN 0.76.x, React 18.3.x.

---

## Task 7: Verificar tsc + tests

**Files:** nenhum (diagnose)

- [ ] **Step 1: Rodar tsc**

Run: `pnpm tsc --noEmit`
Expected: clean OU erros específicos sobre imports. Se houver erros:
  - Ver se são sobre `react-native-video` (esperado — vamos ajustar no Task 10)
  - Ver se são sobre `@react-navigation/material-top-tabs` (esperado — ajustar no Task 11)
  - Outros erros: investigar e reportar

- [ ] **Step 2: Rodar tests**

Run: `pnpm test`
Expected: 42 tests pass. Se algum falhar, investigar.

Possíveis problemas:
- Jest preset diferente no RN 0.76: se o preset base mudou, ajustar `jest.config.js`
- transformIgnorePatterns: ajustar se novo jest-preset de RN 0.76 tiver patterns diferentes

- [ ] **Step 3: Commit se tsc e tests OK**

```bash
git add -A
git commit -m "chore: migrate React Native 0.85 -> 0.76 (scaffold + deps + customizations)"
```

Se houver issues restantes (vídeo, material-tabs), elas serão resolvidas nos próximos tasks. Commit mesmo se parcial; branch de backup está salva.

---

## Task 8: Rebuild Android + install no device

**Files:** nenhum (build)

- [ ] **Step 1: Start Metro**

Em um terminal:
```bash
pnpm start
```
Deixar rodando. Noutro terminal:

- [ ] **Step 2: Connect device + adb reverse**

```bash
export PATH=$PATH:$HOME/Android/Sdk/platform-tools
adb devices
adb reverse tcp:8081 tcp:8081
```

Expected: device visível.

- [ ] **Step 3: Clean + build Android**

```bash
cd android && ./gradlew clean && cd ..
pnpm android 2>&1 | tail -40
```

Expected: `BUILD SUCCESSFUL` + APK instalado + app inicia.

Se build falhar com erros de Kotlin/Gradle version: investigar versões no `android/build.gradle`. RN 0.76 scaffold vem com valores corretos pra RN 0.76.

Se `pnpm android` não encontrar o celular: remontar cabo (problema conhecido do USB do Moto G60), rodar `adb kill-server && adb start-server`.

- [ ] **Step 4: Smoke test básico**

No device, confirmar:
- App abre sem crash
- Tab Exercícios funciona (lista, filtros, busca, detalhe)
- Tab Treinos funciona (criar, listar, editar)
- Tab Histórico funciona (placeholder ou com dados)
- Tab Música ainda é placeholder

Se algum crash, `adb logcat -d | grep -iE 'reactnative|fatal|error' | tail -30` pra diagnosticar.

- [ ] **Step 5: Commit se tudo OK**

```bash
git add -A
git commit -m "chore: verify RN 0.76 app runs on device" --allow-empty
```

---

## Task 9: Restaurar vídeo real em ExerciseMedia

**Files:**
- Modify: `src/components/exercise/ExerciseMedia.tsx`

**Objetivo:** Antes da migração, ExerciseMedia só mostrava placeholder (Known Issue Fase 2). Agora com RN 0.76 + old arch, `react-native-video` v6 funciona normalmente.

- [ ] **Step 1: Substituir `src/components/exercise/ExerciseMedia.tsx` inteiro**

```typescript
import React from 'react';
import { View, StyleSheet } from 'react-native';
import Video from 'react-native-video';
import Icon from 'react-native-vector-icons/MaterialIcons';
import { resolveMedia } from '@/database/mediaResolver';
import { colors, radius } from '@/theme';

interface Props {
  filename: string | null;
  size: number;
  paused?: boolean;
}

export function ExerciseMedia({ filename, size, paused = false }: Props) {
  const source = resolveMedia(filename);

  if (!source) {
    return (
      <View style={[styles.placeholder, { width: size, height: size }]}>
        <Icon name="fitness-center" size={Math.min(72, size * 0.3)} color={colors.textSecondary} />
      </View>
    );
  }

  return (
    <Video
      source={source as any}
      style={{ width: size, height: size, backgroundColor: colors.primaryLight, borderRadius: radius.lg }}
      repeat
      muted
      paused={paused}
      resizeMode="cover"
      playInBackground={false}
    />
  );
}

const styles = StyleSheet.create({
  placeholder: {
    backgroundColor: colors.primaryLight,
    borderRadius: radius.lg,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

- [ ] **Step 2: Verificar tsc**

Run: `pnpm tsc --noEmit`
Expected: clean.

- [ ] **Step 3: Rebuild + test no device**

Run: `pnpm android`

No device: abrir um exercício da biblioteca que tenha MP4. Vídeo deve tocar em loop silencioso. Scroll pelos exercícios — não deve crashar com "InsufficientResources" porque `ExerciseCard` usa ícone estático, não video.

Se funcionar: 🎉 Known Issue Fase 2 resolvida.

- [ ] **Step 4: Commit**

```bash
git add src/components/exercise/ExerciseMedia.tsx
git commit -m "feat: restore real video playback in ExerciseMedia (RN 0.76 unlock)"
```

---

## Task 10: Restaurar material-top-tabs em HistoryTabsScreen

**Files:**
- Modify: `src/screens/history/HistoryTabsScreen.tsx`

**Objetivo:** Substituir toggle custom por `createMaterialTopTabNavigator` (agora funciona porque `react-native-pager-view` v7 é compat com 0.76).

- [ ] **Step 1: Substituir `src/screens/history/HistoryTabsScreen.tsx` inteiro**

```typescript
import React from 'react';
import { createMaterialTopTabNavigator } from '@react-navigation/material-top-tabs';
import { HistoryCalendarScreen } from './HistoryCalendarScreen';
import { StatsScreen } from './StatsScreen';
import { colors, typography } from '@/theme';

const Tab = createMaterialTopTabNavigator();

export function HistoryTabsScreen() {
  return (
    <Tab.Navigator
      screenOptions={{
        tabBarStyle: { backgroundColor: colors.primary },
        tabBarIndicatorStyle: { backgroundColor: colors.accent },
        tabBarLabelStyle: { ...typography.body, fontWeight: '600', textTransform: 'none' },
        tabBarActiveTintColor: colors.accent,
        tabBarInactiveTintColor: colors.textSecondary,
      }}
    >
      <Tab.Screen name="Calendar" component={HistoryCalendarScreen} options={{ title: 'Histórico' }} />
      <Tab.Screen name="Stats" component={StatsScreen} options={{ title: 'Estatísticas' }} />
    </Tab.Navigator>
  );
}
```

- [ ] **Step 2: Verificar tsc**

Run: `pnpm tsc --noEmit`
Expected: clean.

- [ ] **Step 3: Rebuild + test no device**

Run: `pnpm android`

No device: abrir tab Histórico. Deve ter tabs "Histórico" / "Estatísticas" no topo com indicador animado, swipe entre as duas, mesma funcionalidade de antes.

- [ ] **Step 4: Commit**

```bash
git add src/screens/history/HistoryTabsScreen.tsx
git commit -m "feat: restore material-top-tabs in HistoryTabsScreen (RN 0.76 unlock)"
```

---

## Task 11: Atualizar specs + ROADMAP com resoluções

**Files:**
- Modify: `docs/superpowers/specs/2026-04-17-fundacao-biblioteca-exercicios-design.md`
- Modify: `ROADMAP.md`

- [ ] **Step 1: Remover "Known Issue" sobre vídeo da spec da Fase 1+2**

Abra `docs/superpowers/specs/2026-04-17-fundacao-biblioteca-exercicios-design.md`. Procure a seção "## Known Issues" com o bloco sobre "Reprodução de vídeo não funciona nesta fase".

Substituir a seção inteira (com o sub-bloco) por:

```markdown
## Known Issues

*(Nenhum conhecido no momento. Vídeo foi restaurado na migração para RN 0.76 — ver `2026-04-19-migracao-rn-076-design.md`.)*
```

- [ ] **Step 2: Atualizar ROADMAP**

Abrir `ROADMAP.md`. Adicionar entrada de migração entre Fase 5 e Fase 6:

```markdown
---

## ✅ Fase 5.9 — Migração para React Native 0.76

Downgrade controlado de RN 0.85 → RN 0.76 (última versão com old arch suportada). Desbloqueia libs maduras: `react-native-video` (vídeo real), `@react-navigation/material-top-tabs` (top tabs nativos), `react-native-track-player` (viável para Fase 6).

**Status:** Concluída (2026-04-19)
**Spec:** `docs/superpowers/specs/2026-04-19-migracao-rn-076-design.md`
**Plano:** `docs/superpowers/plans/2026-04-19-migracao-rn-076.md`

---
```

(Inserir antes da "## ⏳ Fase 6 — Player de música".)

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-04-17-fundacao-biblioteca-exercicios-design.md ROADMAP.md
git commit -m "docs: mark video playback issue resolved + RN 0.76 migration complete in roadmap"
```

---

## Task 12: Smoke test completo no device

**Files:** nenhum (validação)

- [ ] **Step 1: Rodar suite de testes**

Run: `pnpm test`
Expected: 42 tests pass.

- [ ] **Step 2: Checklist no device — revalidar todas as fases**

Abrir o app e passar por cada tela:

**Fase 2 (Biblioteca):**
- [ ] Tab Exercícios lista ~45 exercícios
- [ ] Busca funciona
- [ ] Filtros (grupo muscular, categoria) funcionam
- [ ] Tocar num exercício com MP4 → vídeo toca em loop ✨ **novo**
- [ ] Exercícios sem MP4 mostram placeholder

**Fase 3 (CRUD treinos):**
- [ ] Tab Treinos lista com favoritos no topo
- [ ] FAB + abre form "Novo treino"
- [ ] Adicionar exercícios via picker funciona
- [ ] Reorder com ▲/▼ funciona
- [ ] Salvar, editar, duplicar, apagar funcionam
- [ ] Toggle favorito no card funciona

**Fase 4 (Execução):**
- [ ] Tap no card → tela de preview com botão Iniciar
- [ ] Iniciar → tela de execução com timer, exercício, inputs
- [ ] "Concluir série" → rest timer com -30/Skip/+30
- [ ] Fim do descanso: vibração + notificação
- [ ] Keep-awake: tela não apaga durante execução
- [ ] Finalizar treino → tela de resumo → Concluir → volta pra lista

**Fase 5 (Histórico):**
- [ ] Tab Histórico tem top tabs material ✨ **novo**
- [ ] Calendário mensal com dias marcados
- [ ] Tap num dia → lista de sessões
- [ ] Tap numa sessão → detalhe com exercícios/séries
- [ ] Botão "Repetir treino" inicia nova sessão
- [ ] Aba Estatísticas: 3 cards (treinos do mês, frequência, duração)

- [ ] **Step 3: Se algo quebrar** — diagnosticar com logcat, consultar commits do backup, consertar e commitar.

- [ ] **Step 4: Commit final vazio**

```bash
git commit --allow-empty -m "chore: RN 0.76 migration verified end-to-end on device"
```

---

## Notas para implementação

- **Branch de backup** (`backup-pre-rn-076-migration`) permite rollback completo se algo der muito errado. Após smoke test OK, pode-se deletar: `git branch -D backup-pre-rn-076-migration`.
- **Arquivo `.gitattributes`**: o scaffold RN 0.76 pode trazer uma versão diferente. Se conflitar, usar o do RN 0.85 (nosso git tem ele). Conteúdo não muda comportamento.
- **pnpm-lock.yaml** vai ser regenerado do zero, não tem como preservar. OK.
- **Metro pode morrer** em background após muito tempo; reiniciar `pnpm start` se necessário.
- **Cabo USB instável** — pode-se gerar APK e instalar manualmente se conexão cair demais: `./gradlew assembleDebug` → copiar APK pra celular.
- **Versões de dep**: se op-sqlite ou notifee nas versões sugeridas não resolverem compat, o implementer pode experimentar versões próximas (ex: op-sqlite 10.x, notifee 7.8.x).
- **Track-player** NÃO é instalado aqui — fica pra Fase 6.
- **Keystore pra release** NÃO é configurado aqui — fica pra Fase 7.
